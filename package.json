{
  "name": "satya-monorepo",
  "version": "1.0.0",
  "description": "Unified Satya news verification platform - Backend, Pipeline, and News Scraper in one",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "dev": "node index.js",
    "install-all": "npm install && cd services/backend && npm install && cd ../pipeline && npm install && cd ../scraper && npm install",
    "build": "cd services/backend && npm install && npm run build && cd ../pipeline && npm install && cd ../scraper && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  },
  "author": "",
  "license": "ISC"
}
